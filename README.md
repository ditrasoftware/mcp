# mcp

Repository for MCP server implementations.

## Structure
- servers/: All MCP implementations

## Local run
From this repository root, set:

```bash
export PYTHONPATH=servers
```

Then run a server module, for example:

```bash
python -m ferreromed_mcp
```
